/* tslint:disable:no-unused-variable */

import { NosignPipe } from "./Nosign.pipe";


describe('Pipe: Nosigne', () => {
  it('create an instance', () => {
    let pipe = new NosignPipe();
    expect(pipe).toBeTruthy();
  });
});
